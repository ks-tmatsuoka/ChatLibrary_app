<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:ChatSample"
    mc:Ignorable="d"
    x:Class="ChatSample.MainPage">

    <ContentPage.BindingContext>
        <local:MainPageViewModel/>
    </ContentPage.BindingContext>

    <StackLayout>
        <!--  Place new controls here  -->
        <Button
            Text="サンプル（標準）"
            Command="{Binding PushPageCommand}"
            CommandParameter="{x:Type local:ChatPage}"
        />

        <Button
            Text="サンプル（カスタマイズ）"
            Command="{Binding PushPageCommand}"
            CommandParameter="{x:Type local:CustomChatPage}"
        />

        <Button
            Text="サンプル（カスタマイズ_関）"
            Command="{Binding PushPageCommand}"
            CommandParameter="{x:Type local:SekiCustomChatPage}"
        />
    </StackLayout>
</ContentPage>